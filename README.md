# MMM-MyPackageTracker



### Logos & fallback
This module pulls brand marks from the **Simple Icons** CDN (CC0-1.0). If a brand is unknown or the CDN is unreachable, the module shows a **neutral fallback icon** (`/MMM-MyPackageTracker/icons/fallback.svg`). The USPS slug is `usps` on Simple Icons.
